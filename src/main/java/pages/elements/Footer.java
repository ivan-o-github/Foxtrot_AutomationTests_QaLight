package pages.elements;

import org.openqa.selenium.WebDriver;
import pages.ActionsWithElements;

public class Footer extends ActionsWithElements {
    public Footer(WebDriver webDriver) {
        super(webDriver);
    }
}
